<?php

/**
 * Password Confirmation Tests (API)
 *
 * JWT-based API does not use session-based password confirmation.
 * The API profile password change flow is tested in ApiCrudTest.
 */

test('placeholder for api password confirmation', function () {
    expect(true)->toBeTrue();
});
